# Spec: <one-line objective>

> ALIGN artifact. WHAT/WHY end-state — never HOW. Durable contract; the
> `plan` step decomposes this into milestones/issues without asking the user
> anything. Every section must be verifiable. No TODO/??? placeholders.

## Objective

<One sentence: what is true when this is done that isn't true now.>

## Why

<What breaks or is lost without this. Who feels the pain. The motivating context.>

## Acceptance criteria

<Numbered, each independently verifiable — a check, test, or measurable number.
A reader must be able to mark each pass/fail without judgment calls.>

1. ...
2. ...

## In scope

- ...

## Out of scope

<At least one explicit exclusion. Prevents the plan step from over-reaching.>

- ...

## Constraints

<Hard limits: compatibility, performance budgets, deadlines, required/forbidden
tech, interop requirements.>

- ...

## Environment / prior art

<What already exists that this should build on, extend, or match — surfaced by
the align explore phase. Relevant modules, prior deliverables of the same kind,
conventions, reference material. Cite specifics (paths, names) so the plan step
inherits the context instead of re-discovering it.>

- ...

## Existing surface (must not break)

<What this touches and what must keep working — verified against the actual
environment during the explore phase, not just recalled by the user. Files,
APIs, workflows, contracts.>

- ...

## Open questions

<Genuinely undecided items. Each needs a decision rule and an owner, OR an explicit
"deferred — does not block planning" note. Should be empty or deferred before the
spec is considered ready for `plan`.>

- ...
